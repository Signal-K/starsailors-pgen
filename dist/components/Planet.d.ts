import type { PlanetConfig } from "../utils/planet-config";
interface PlanetProps {
    config: PlanetConfig;
}
export default function Planet({ config }: PlanetProps): import("react/jsx-runtime").JSX.Element | null;
export {};
